{
  "name": "discord-hono",
  "version": "0.5.3",
  "description": "This module enables you to easily build a Discord Bot that runs on Cloudflare workers",
  "author": "Luis (https://github.com/LuisFun)",
  "homepage": "https://github.com/LuisFun/discord-hono",
  "license": "MIT",
  "keywords": ["discord-hono", "discord-bot", "cloudflare-workers"],
  "files": ["dist"],
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "fix": "npx @biomejs/biome check --apply .",
    "fix:unsafe": "npx @biomejs/biome check --apply-unsafe .",
    "patch": "npm run fix && npm version patch && git push --follow-tags",
    "minor": "npm run fix && npm version minor && git push --follow-tags",
    "major": "npm run fix && npm version major && git push --follow-tags",
    "esbuild:cjs": "esbuild --bundle ./src/index.ts --outfile=./dist/index.cjs --format=cjs",
    "esbuild:esm": "esbuild --bundle ./src/index.ts --outfile=./dist/index.mjs --format=esm",
    "build": "rimraf dist && tsc && npm run esbuild:cjs && npm run esbuild:esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "discord-api-types": "^0.37.81",
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  }
}
