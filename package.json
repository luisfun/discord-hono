{
  "name": "discord-hono",
  "version": "0.6.0",
  "description": "This module enables you to easily build a Discord Bot that runs on Cloudflare workers",
  "author": "Luis (https://github.com/LuisFun)",
  "homepage": "https://github.com/LuisFun/discord-hono",
  "license": "MIT",
  "keywords": [
    "discord-hono",
    "discord-bot",
    "cloudflare-workers"
  ],
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "fix": "npx @biomejs/biome check --apply .",
    "fix:unsafe": "npx @biomejs/biome check --apply-unsafe .",
    "patch": "npm run fix && npm run build && npm version patch && git push --follow-tags",
    "minor": "npm run fix && npm run build && npm version minor && git push --follow-tags",
    "major": "npm run fix && npm run build && npm version major && git push --follow-tags",
    "build:dts": "tsc && dts-bundle-generator ./dist/types/index.d.ts -o ./dist/index.d.ts && rimraf ./dist/types",
    "build:esm": "esbuild ./src/index.ts --outdir=./dist --bundle --platform=node --format=esm",
    "build": "rimraf ./dist && npm run build:dts && npm run build:esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "discord-api-types": "^0.37.83",
    "dts-bundle-generator": "^9.5.1",
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  }
}
